# DataProcessor

![Flask](https://img.shields.io/badge/Flask-2.3.3-green.svg)
![Python](https://img.shields.io/badge/Python-3.8+-blue.svg)

Веб-приложение для обработки и анализа структурированных данных в формате JSON.
Профессиональный инструмент с современным веб-интерфейсом и RESTful API.

## Основные возможности

- **Загрузка JSON-файлов** с валидацией структуры и формата
- **Отслеживание выполнения** в реальном времени с прогресс-баром
- **Детальная аналитика** обработки данных
- **Журналирование** с фильтрацией по уровням важности
- **Управление результатами** с возможностью экспорта
- **Полная документация** и справочная система

## Технологический стек

- **Backend**: Flask 2.3.3, Python 3.8+
- **Frontend**: HTML5, CSS3, JavaScript (ES6+)
- **Архитектура**: Modular Blueprints, RESTful API
- **Стили**: CSS Grid, Flexbox, CSS Variables
- **Обработка ошибок**: Кастомные обработчики и исключения

## Установка и запуск

### Предварительные требования

- Python 3.8 или выше
- pip (менеджер пакетов Python)

### 1. Клонирование репозитория

```bash
git clone git@github.com:AndB0ndar/DataProcessor.git
cd data_processor
```

### 2. Создание виртуального окружения

```bash
python -m venv venv
source venv/bin/activate  # Linux/MacOS
# или
venv\Scripts\activate  # Windows
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Настройка конфигурации

Создайте файл `.env` (опционально):

```bash
FLASK_CONFIG=development
SECRET_KEY=your-secret-key-here
FLASK_HOST=0.0.0.0
FLASK_PORT=5000
```

### 5. Запуск приложения

```bash
python app.py
```

или

```bash
flask run
```

Приложение будет доступно по адресу: `http://localhost:5000`

## Использование

### Основной workflow

1. **Загрузка данных** - Перейдите на главную страницу и загрузите JSON-файл
2. **Валидация** - Система автоматически проверит структуру файла
3. **Обработка** - Запустите процесс анализа данных
4. **Мониторинг** - Отслеживайте прогресс на странице статуса
5. **Результаты** - Просмотрите и скачайте результаты обработки

### Требования к JSON-файлу

```json
{
  "version": "1.0",
  "timestamp": "2024-01-15T10:30:00Z",
  "metadata": {
    "source": "data_source",
    "description": "Описание данных"
  },
  "data": [
    {
      "id": 1,
      "name": "Пример записи",
      "value": 42.5,
      "category": "A"
    }
  ]
}
```

**Обязательные поля:**
- `version` - версия формата данных
- `timestamp` - метка времени в формате ISO 8601
- `data` - массив объектов для обработки

## 🔧 Конфигурация

### Режимы работы

- **development** - Режим разработки с детальным логированием
- **production** - Продакшен режим с оптимизацией безопасности
- **testing** - Режим тестирования с изолированными папками

### Переменные окружения

| Переменная | По умолчанию | Описание |
|------------|--------------|----------|
| `FLASK_CONFIG` | `development` | Режим работы приложения |
| `SECRET_KEY` | - | Секретный ключ для сессий |
| `FLASK_HOST` | `0.0.0.0` | Хост для запуска |
| `FLASK_PORT` | `5000` | Порт для запуска |

### Ограничения

- Максимальный размер файла: 10MB (в development), 5MB (в production)
- Максимальное количество записей: 100,000 (в development), 50,000 (в production)
- Поддерживаемые форматы: JSON

## API Endpoints

### Основные endpoints

| Метод | Endpoint | Описание |
|-------|----------|----------|
| `POST` | `/api/upload` | Загрузка JSON-файла |
| `POST` | `/api/start_processing` | Запуск обработки данных |
| `GET` | `/api/processing_status/<task_id>` | Статус обработки |
| `GET` | `/api/results` | Список результатов |
| `GET` | `/api/logs` | Журнал выполнения |

### Пример использования API

```python
import requests

# Загрузка файла
with open('data.json', 'rb') as f:
    response = requests.post('http://localhost:5000/api/upload', files={'file': f})
    task_id = response.json()['task_id']

# Запуск обработки
requests.post('http://localhost:5000/api/start_processing', 
              json={'task_id': task_id})

# Проверка статуса
status = requests.get(f'http://localhost:5000/api/processing_status/{task_id}')
```

## Структура проекта

```
data_processor/
├── app.py                # Основной файл приложения
├── config.py             # Конфигурация приложения
├── requirements.txt      # Зависимости Python
├── handlers/             # Обработчики ошибок
│   ├── __init__.py
│   └── errors.py
├── routes/               # Маршруты приложения
│   ├── __init__.py
│   ├── website.py        # Основные маршруты
│   └── api.py            # API endpoints
├── static/               # Статические файлы
│   ├── css/
│   │   └── style.css
│   └── js/
│       └── main.js
└── templates/            # HTML шаблоны
    ├── base.html
    ├── index.html
    ├── status.html
    ├── logs.html
    ├── results.html
    ├── help.html
    └── errors/           # Шаблоны страниц ошибок
```

## Интерфейс

### Цветовая схема

Приложение использует современную цветовую палитру:

- **Основной зеленый**: `#87A96B`
- **Кремовый**: `#FEFAE0`
- **Акцентный оранжевый**: `#BC6C25`
- **Мятный**: `#A8D5BA`
- **Лесной зеленый**: `#2E8B57`
- **Светло-серый**: `#F8F9FA`
- **Тёмно-сланцевый**: `#34495E`
- **Тёмно-синий**: `#1F2937`
- **Янтарный**: `#F59E0B`

### Адаптивный дизайн

- Полная поддержка мобильных устройств
- Гибкая сетка на CSS Grid и Flexbox
- Плавные анимации и переходы

## CLI команды

```bash
# Очистка папки загрузок
flask clear-uploads

# Проверка конфигурации
flask check-config

# Инициализация БД (если будет добавлена)
flask init-db
```

## Тестирование

Для запуска тестов установите зависимости разработки:

```bash
pip install pytest pytest-flask
```

Запуск тестов:

```bash
export FLASK_CONFIG=testing
pytest
```

## Лицензия

Не определена

---

**DataProcessor** - профессиональное решение для обработки структурированных данных с открытым исходным кодом.
